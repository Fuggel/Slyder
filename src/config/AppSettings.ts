interface IAppSettings {
  iconBig: number;
  iconSmall: number;
  darkMode: boolean;
  wifi: boolean;
}

export const appSettings: IAppSettings = {
  iconBig: 28,
  iconSmall: 22,
  darkMode: true,
  wifi: false,
};
